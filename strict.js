/*
 * strict mode kod yapısını sıkı tutmak için kullanılan bir direktif
 *
 * "use strict"; şeklinde kullanılır
 * 
 * Beyan edilmemiş değişkenler kullanılamaz
 * Delete anahtar kelimesinin kullanılmasınada izin vermez
 * Fonksiyon parametrelerinde kopya isimlere izin vermez
 * Oktal sayısal değişkenlerede izin vermez
 * Oktal kaçış karakterlerinede izin vermez
 * Read-only obje özelliklerinin değiştirilmesine izin vermez
 * Get-only obje özelliklerinin değiştirilmesine izin vermez
 * Silinemez özelliklerin silinmesine izin vermez
 * eval kelimesi değişken ismi olarak kullanılamaz
 * arguments kelimesi değişken ismi olarak kullanılamaz
 * with ifadesine izin vermez
 * eval()'in çağırıldığı kapsamda değişken oluşturmasına izin vermez
 * this anahtar kelimesi strict mode'da fonksiyon içerisinde kullanıldığında undefined döndürür
 * normal modda window objesini döndürür
 * Eğer fonksiyon bir obje constructor olarak kullanıldıysa objeyi döndürür
 * 
 * strict mode sonraki güncellemelerde gelecek anahtar kelimelerinde kullanılmasına izin vermez:
 * implements,
 * interface,
 * package,
 * private,
 * protected,
 * public,
 * static,
 * yield
 *  
 */
